{
  "name": "@exactly/plugin",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "prepare": "[ \"$EAS_BUILD_RUNNER\" != eas-build ] || (export FOUNDRY_DIR=${FOUNDRY_DIR-$HOME/workingdir} && (command -v foundryup >/dev/null || { curl -fsSL --retry 3 --retry-all-errors https://foundry.paradigm.xyz | bash; command -v foundryup >/dev/null; }) && (v=v1.5.1 && for i in $(seq 1 3); do foundryup -i $v && break; [ $i -eq 3 ] && exit 1; sleep $((i * 2)); done))",
    "snapshot": "FOUNDRY_PROFILE=snapshot forge snapshot --force",
    "test": "nx test contracts",
    "test:fmt": "forge fmt --check",
    "test:gas": "FOUNDRY_PROFILE=snapshot forge snapshot --force --check",
    "test:solhint": "solhint --max-warnings 0 '**/*.sol'",
    "test:slither": "slither . --skip-clean",
    "test:coverage": "forge coverage --report lcov",
    "test:sizes": "forge build --sizes --skip test --skip script"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "modular-account": "alchemyplatform/modular-account#c81e7122eb46e13447afa3f2e0d2537cf2792beb",
    "modular-account-libs": "erc6900/modular-account-libs#v0.7.1",
    "solady": "^0.1.26",
    "webauthn-owner-plugin": "exactly/webauthn-owner-plugin#f52b3f2",
    "webauthn-sol": "base/webauthn-sol#v1.0.0"
  },
  "devDependencies": {
    "@exactly/protocol": "^0.2.22",
    "@openzeppelin/contracts-upgradeable-v4": "npm:@openzeppelin/contracts-upgradeable@^4.9.6",
    "@openzeppelin/contracts-upgradeable": "^5.6.1",
    "@openzeppelin/contracts-v4": "npm:@openzeppelin/contracts@^4.9.6",
    "account-abstraction": "eth-infinitism/account-abstraction#v0.6.0",
    "forge-std": "foundry-rs/forge-std#v1.15.0",
    "fresh-crypto-lib": "rdubois-crypto/FreshCryptoLib#fd2a0e6e64609ade0b0c165e4d66de8106a70db6",
    "solhint": "^6.1.0",
    "solmate": "transmissions11/solmate#v7",
    "surl": "memester-xyz/surl",
    "typescript": "catalog:"
  },
  "nx": {
    "projectType": "library",
    "namedInputs": {
      "production": [
        "default",
        "!{projectRoot}/test/**/*",
        "!{projectRoot}/broadcast/*/31337/**/*"
      ]
    },
    "targets": {
      "build": {
        "parallelism": false,
        "outputs": [
          "{projectRoot}/out",
          "{projectRoot}/cache"
        ]
      },
      "snapshot": {
        "outputs": [
          "{projectRoot}/.gas-snapshot"
        ],
        "cache": true
      },
      "test": {
        "dependsOn": [
          "test:fmt",
          "test:solhint",
          "test:sizes",
          "test:gas",
          "test:slither",
          "test:coverage"
        ],
        "executor": "nx:noop"
      },
      "test:sizes": {
        "parallelism": false,
        "outputs": [
          "{projectRoot}/out",
          "{projectRoot}/cache"
        ]
      },
      "test:gas": {
        "parallelism": false,
        "outputs": [
          "{projectRoot}/out",
          "{projectRoot}/cache",
          "{projectRoot}/.gas-snapshot"
        ]
      },
      "test:slither": {
        "parallelism": false,
        "outputs": [
          "{projectRoot}/out",
          "{projectRoot}/cache"
        ]
      },
      "test:coverage": {
        "parallelism": false,
        "outputs": [
          "{projectRoot}/out",
          "{projectRoot}/cache",
          "{projectRoot}/lcov.info"
        ]
      }
    }
  }
}
